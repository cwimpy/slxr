% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{slx_plot_shock}
\alias{slx_plot_shock}
\title{Counterfactual shock plot}
\usage{
slx_plot_shock(fit, variable, unit, magnitude = 1, geom = NULL, top_n = 15)
}
\arguments{
\item{fit}{A cross-sectional \code{slx} model. Panel shocks are planned
for a future release.}

\item{variable}{Character, the name of a spatially-lagged regressor
in \code{fit} to shock.}

\item{unit}{Integer row index (or character id, if the weights
matrix has dimnames matching something in \code{fit$data}) of the unit
receiving the shock.}

\item{magnitude}{Numeric, shock size. Default \code{1}.}

\item{geom}{Optional \code{sf} object with \code{nrow(geom) == fit$n} aligned
to \code{fit$data}. If supplied, the function returns a map.}

\item{top_n}{For the non-map plot, how many non-zero indirect
effects to show. Default \code{15}.}
}
\value{
A \code{ggplot} object.
}
\description{
Given a fitted SLX model, a choice of variable, and a target unit,
returns a plot of the predicted change in the outcome across every
unit in the sample under a unit shock to that variable in the
target unit.
}
\details{
For an SLX model at first order with channels indexed by \code{c}, the
predicted change at unit \code{j} from a shock of size \code{magnitude} to
variable \code{x} in unit \code{i} is
\deqn{\text{magnitude}\ \left(\beta\ \mathbb{1}\{j=i\} + \sum_c \theta_c\ W_c[j, i]\right).}
Higher-order lags add additional \verb{theta_\{c,k\} (W_c^k)[j, i]} terms.
No simulation is required: the shock effect is a single column of
the spatial multiplier.

If an \code{sf} object with matching row count is supplied via \code{geom},
the result is drawn as a choropleth. Otherwise a horizontal bar of
the largest effects is returned.
}
\examples{
\dontrun{
data(defense_burden)
W_c <- slx_weights(style = "custom", matrix = defense_burden$W_contig,
                   row_standardize = FALSE)
fit <- slx(ch_milex ~ milex_tm1 + civilwar_tm1,
           data = defense_burden$data, W = W_c,
           lag = "civilwar_tm1")
slx_plot_shock(fit, variable = "civilwar_tm1", unit = 1)
}
}
