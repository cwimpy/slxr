% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{slx_weights}
\alias{slx_weights}
\title{Construct a spatial weights matrix}
\usage{
slx_weights(
  x = NULL,
  style = c("contiguity", "rook", "knn", "distance", "custom"),
  k = 5,
  threshold = NULL,
  row_standardize = TRUE,
  matrix = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{sf} object (for \code{"contiguity"}, \code{"knn"}, \code{"distance"}), a
matrix of coordinates, or a raw numeric/sparse matrix (for
\code{"custom"}).}

\item{style}{Weights style. One of \code{"contiguity"} (queen), \code{"rook"},
\code{"knn"}, \code{"distance"}, or \code{"custom"}.}

\item{k}{Number of neighbors for \code{style = "knn"}.}

\item{threshold}{Distance threshold for \code{style = "distance"} (units of
the coordinate system).}

\item{row_standardize}{Logical; row-standardize the matrix? Default
\code{TRUE}. The paper notes row-standardization is a theoretical choice;
set to \code{FALSE} when connection count should itself carry weight.}

\item{matrix}{A numeric or sparse \code{Matrix} for \code{style = "custom"}.}

\item{...}{Passed to the underlying \code{spdep} constructor.}
}
\value{
An object of class \code{slx_W} with elements \code{W} (sparse matrix),
\code{listw} (spdep \code{listw} object), \code{style}, and \code{row_standardized}.
}
\description{
A thin, opinionated wrapper around common \code{spdep} weights constructors.
Returns a standardized \code{slx_W} object that carries both the sparse matrix
and the \code{listw} form used by downstream routines.
}
\examples{
\dontrun{
library(sf)
nc <- st_read(system.file("shape/nc.shp", package = "sf"))
W  <- slx_weights(nc, style = "contiguity")
}
}
