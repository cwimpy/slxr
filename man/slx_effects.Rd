% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effects.R
\name{slx_effects}
\alias{slx_effects}
\title{Direct, indirect, and total effects from an SLX model}
\usage{
slx_effects(object, by_order = FALSE, conf.level = 0.95)
}
\arguments{
\item{object}{An \code{slx} object returned by \code{\link[=slx]{slx()}}.}

\item{by_order}{Logical; if \code{TRUE}, report indirect effects separately
by order of W. Default \code{FALSE} sums across orders for a single
indirect effect per variable–W combination.}

\item{conf.level}{Confidence level for reported intervals. Default 0.95.}
}
\value{
A tibble with columns \code{variable}, \code{w_name}, \code{order} (when
\code{by_order = TRUE}), \code{type} (direct/indirect/total), \code{estimate},
\code{std.error}, \code{conf.low}, \code{conf.high}, \code{p.value}.
}
\description{
For an SLX model the effects decomposition is trivial — no matrix
inversion, no simulation. For each variable \eqn{x} that enters both
directly and as a spatial lag, the direct effect is its OLS coefficient
\eqn{\hat\beta} and the indirect effect at order \eqn{k} is
\eqn{\hat\theta_k}. Standard errors come straight from \code{vcov()}.
}
\examples{
data(defense_burden)
W <- slx_weights(style = "custom", matrix = defense_burden$W_contig,
                 row_standardize = FALSE)
fit <- slx(ch_milex ~ milex_tm1 + civilwar_tm1,
           data = defense_burden$data, W = W, lag = "civilwar_tm1")
slx_effects(fit)
}
