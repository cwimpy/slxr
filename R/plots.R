#' Coefficient plot of direct, indirect, and total effects
#'
#' Stub — v0.2. Planned: a ggplot-based coefficient plot summarizing the
#' output of [slx_effects()].
#'
#' @param fit An `slx` model.
#' @export
#' @keywords internal
slx_plot_effects <- function(fit) {
  stop("slx_plot_effects() is not yet implemented.", call. = FALSE)
}

#' Plot how indirect effects decay across orders of W
#'
#' Stub — v0.2.
#'
#' @param fit An `slx` model fit with higher-order `W` terms.
#' @export
#' @keywords internal
slx_plot_decay <- function(fit) {
  stop("slx_plot_decay() is not yet implemented.", call. = FALSE)
}

#' Heatmap of a spatial weights matrix
#'
#' Stub — v0.2.
#'
#' @param W An `slx_W` object.
#' @export
#' @keywords internal
slx_plot_W <- function(W) {
  stop("slx_plot_W() is not yet implemented.", call. = FALSE)
}
